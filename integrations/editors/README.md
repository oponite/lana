# Editor integrations

VS Code and Neovim are the tested Lana 2.0 editor integrations. Other
LSP-capable editors can launch this command for `.lana` files:

```bash
lana lsp
```

Use `lana.toml` or the repository root as the workspace root. The current
server reports diagnostics for saved files.
