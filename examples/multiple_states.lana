state observation = state(p: 0.90, d: 0.40);
state report = state(p: 0.70, d: 0.60);
state belief = state(p: 0.20, d: 0.10);

let evidence = append(observation, report);
let combined = append(evidence, belief);
let concrete = sample(combined);
print(measure concrete as probability);
